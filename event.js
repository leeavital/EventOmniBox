/**
 * class used to represent and Event
 */
function Event(){

	this.name; // string
	this.datetime = new DateTime(); // DateTime();
	this.location; // Location();
	this.tags = []; // array of random words we don't know what to do with.
	this.murmer; // int
}
